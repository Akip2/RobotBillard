const width = 700;
const height = 300;

const wallSize = 20;

const ballRadius = 10;
const ballColors = [
    "#FFD700", // yellow
    "#0000FF", // blue
    "#FF0000", // red
    "#8B00FF", // violet
    "#FFA500", // orange
    "#008000", // green
    "#8B0000", // brown
];

const holeRadius = ballRadius * 1.5;

const robotWidth = 30;
const robotHeight = 30;
const wheelRadius = 6.5;

export {
    ballRadius,
    ballColors,
    holeRadius,
    wallSize,
    width,          // table width
    height,         // table height
    robotWidth,
    robotHeight,
    wheelRadius
}