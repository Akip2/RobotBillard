const ballSize=10;
const ballColors=["blue", "yellow", "red", "purple", "orange", "pink", "black"];

const holeSize=ballSize*1.5;

const width=720;
const height=350;

export{
    ballSize,
    ballColors,
    holeSize,
    width,
    height
}