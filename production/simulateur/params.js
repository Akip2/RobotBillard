const width=700;
const height=300;

const wallSize=20;

const ballRadius=10;
const ballColors=[
    "#FFD700", // jaune
    "#0000FF", // bleu
    "#FF0000", // rouge
    "#8B00FF", // violet
    "#FFA500", // orange
    "#008000", // vert
    "#8B0000", // marron/bordeaux
];

const holeRadius=ballRadius*1.5;

const robotWidth=30;
const robotHeight=30;
const wheelRadius=6.5;

export{
    ballRadius,
    ballColors,
    holeRadius,
    wallSize,
    width,
    height,
    robotWidth,
    robotHeight,
    wheelRadius
}